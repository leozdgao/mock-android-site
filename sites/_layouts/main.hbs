<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <!-- 样式 -->
    {{#each assets.links}}
    <link rel="stylesheet" href="{{this}}">
    {{/each}}
    <title>{{title}}</title>
  </head>
  <body>
    {{> navbar/index}}
    {{{body}}}
    <!-- 页面的公共脚本或者trace服务的脚本可以放在这里加载 -->
    {{#each assets.scripts}}
    <script src="{{this}}"></script>
    {{/each}}
  </body>
</html>
